<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.kwater.hdcs.setting.dao.SettingDAO">

    <select id="selectDeviceConfigList" resultType="kr.kwater.hdcs.setting.vo.DeviceConfigVO">
        SELECT *
        FROM public.v_device_config
        ORDER BY device_id
    </select>

    <select id="selectGroupNames" resultType="string">
        SELECT group_name
        FROM public.tb_device_group
        WHERE use_yn = 'Y'
        ORDER BY sort_order, group_name
    </select>

    <!-- CSV 등록: 장비 조회 (device_name = equipId) -->
    <select id="selectDeviceForCsv" parameterType="string" resultType="map">
        SELECT device_id  AS "deviceId",
               station_id AS "stationId"
        FROM public.tb_device
        WHERE device_name = #{equipId}
        LIMIT 1
    </select>

    <!-- 기존 관측소 이름·코드 업데이트 -->
    <update id="updateStationById" parameterType="map">
        UPDATE public.tb_observation_station
        SET name     = #{name},
            obs_code = #{obsCode}
        WHERE station_id = #{stationId}
    </update>

    <!-- 관측소 신규 생성 (generated station_id 반환) -->
    <insert id="insertStation" parameterType="map"
            useGeneratedKeys="true" keyProperty="stationId" keyColumn="station_id">
        INSERT INTO public.tb_observation_station (obs_code, name)
        VALUES (#{obsCode}, #{name})
    </insert>

    <!-- 장비에 관측소 연결 -->
    <update id="updateDeviceStationId" parameterType="map">
        UPDATE public.tb_device
        SET station_id = #{stationId}
        WHERE device_name = #{equipId}
    </update>

</mapper>
